# engine.py 的 Python 依赖。PyTorch 要先从 CUDA 源单独装（见 setup.ps1），
# 这里不写 torch：从 PyPI 装到的是纯 CPU 版。
#
# Qwen3-ASR 直接吃解码好的 16 kHz NumPy PCM，不需要额外的音频解码库。
#
# llama-cpp-python 不在这里：它只给「从修改中学词」用，听写链路不经过它，
# 而 PyPI 上的是要本地编译的纯 CPU 版。需要的话自己装 CUDA 版。
transformers==5.14.1
accelerate==1.14.0
numpy==2.4.6
scipy==1.17.1
soundfile==0.14.0
websockets==17.0.1
onnxruntime==1.28.0
opencc-python-reimplemented==0.1.7
imageio-ffmpeg==0.6.0
